<view class="revice_address_row">
   <view class="address_btn"  wx:if="{{!address.userName}}" >
      <!-- <button bindtap="handleChooseAddress" >配送至: -->
        <!-- <image class="mapInsertLocation" mode="scaleToFill" src="https://media.unioncityitaly.com/mapInsertLocation.svg"></image> -->
        <navigator class="addressnavigator" url="/pages/search/index"> 配送至: </navigator>
      <!-- </button> -->
    </view>
    <view wx:else class="user_info_row">
      <view class="user_info">
        <view>{{address.userName}}</view>
        <view>{{address.all}}</view>
   </view>
   <view class="user_phone">{{address.telNumber}}</view>
   </view>
</view>

